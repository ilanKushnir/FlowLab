# =============================================================================
# 🧪 FlowLab - Your Personal Automation Laboratory
# =============================================================================
# Copy this file to 'config.env' and customize with your settings
# cp config.env.example config.env

# =============================================================================
# RASPBERRY PI CONNECTION
# =============================================================================
# Replace with YOUR Raspberry Pi details
PI_HOST=192.168.1.100               # Your Pi's IP address (find with: ip addr or nmap)
PI_USER=pi                          # SSH username (usually 'pi')
PI_SSH_KEY=~/.ssh/id_ed25519        # Path to your SSH private key

# =============================================================================
# SYSTEM CONFIGURATION
# =============================================================================
# Timezone & Locale - change to your location
TZ=America/New_York                 # Examples: Europe/London, Asia/Tokyo, Australia/Sydney
LANG=en_US.UTF-8

# Network Configuration
DOCKER_NETWORK=homeai_network

# Volume Paths (on Raspberry Pi) - usually no need to change
DATA_ROOT=/opt/homeai/data

# =============================================================================
# SERVICES CONFIGURATION - INTERNAL NETWORKING
# =============================================================================
# Services communicate internally via container names
# External access via PI_HOST:PORT

# N8N Configuration
N8N_PORT=5678
N8N_WEBHOOK_URL=http://${PI_HOST}:${N8N_PORT}

# PostgreSQL Configuration  
POSTGRES_PORT=5432
POSTGRES_DB=n8n
POSTGRES_USER=n8n
# No password needed - trust authentication for internal network

# Freqtrade Configuration
FREQTRADE_PORT=8081
FREQTRADE_WEBSERVER_PORT=8080

# SearxNG Configuration
SEARXNG_PORT=8080

# Portainer Configuration
PORTAINER_PORT=9000



# =============================================================================
# WATCHTOWER - AUTOMATIC UPDATES
# =============================================================================
WATCHTOWER_POLL_INTERVAL=86400           # Check for updates every 24 hours
WATCHTOWER_SCHEDULE="0 2 * * *"          # Run daily at 2 AM (cron format)
WATCHTOWER_CLEANUP=true                  # Remove old images
WATCHTOWER_INCLUDE_STOPPED=true          # Update stopped containers
WATCHTOWER_ROLLING_RESTART=true          # Update one at a time

# =============================================================================
# CUSTOMIZATION OPTIONS
# =============================================================================
# Change ports if you have conflicts (uncomment and modify as needed)
# N8N_PORT=5679
# SEARXNG_PORT=8081
# FREQTRADE_PORT=8082
# PORTAINER_PORT=9001

# Different timezone examples:
# TZ=Europe/London          # UK
# TZ=Europe/Berlin          # Germany  
# TZ=Asia/Tokyo             # Japan
# TZ=Asia/Jerusalem         # Israel
# TZ=Australia/Sydney       # Australia
# TZ=America/Los_Angeles    # US West Coast
# TZ=America/Chicago        # US Central
# TZ=America/New_York       # US East Coast 